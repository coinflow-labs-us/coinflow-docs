import ContentBlock from '../../../components/ContentFromFile/ContentBlock';

<ContentBlock filePath="package.json" highlight="10-13">

# Prerequisites
Before we get started, you need to install the below Eth libraries:

```
npm install ethers@5.7.2
npm install @web3modal/ethereum
npm install @web3modal/react wagmi
```

</ContentBlock>
