import CodeFromFile from '@site/src/components/CodeFromFile';

export default function Code() {
    return <CodeFromFile language={'react'} blockchain={'solana'} product={'checkout'} file={'src/App.tsx'}/>;
}
